import React from 'react'
import styles from './Loading.module.less'

const Loading = () => {
	return <div className={styles.loading}>
		<div className={styles.loading_main}></div>
	</div>
}

export default Loading
